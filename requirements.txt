# Core service -- everything the gateway + tests need. No AWS deps required
# for local mode (boto3 is imported lazily, only with CONDUIT_USE_REAL_AWS=1).
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.6
pytest>=8.0
httpx>=0.27

# Real providers (lazy imports -- only needed when the matching key is set)
anthropic>=0.40
openai>=1.40

# Real-AWS mode only (uncomment when deploying against real DynamoDB)
# boto3>=1.34
